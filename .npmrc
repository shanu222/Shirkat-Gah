# pnpm workspace — required for Vercel monorepo installs
# https://pnpm.io/npmrc
auto-install-peers=true
strict-peer-dependencies=false

# Ensure workspace packages are linked (not isolated from monorepo root)
link-workspace-packages=true
